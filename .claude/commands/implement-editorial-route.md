---
description: Implement or refine one existing Caught Up editorial route end-to-end.
argument-hint: <route>
---

Implement `$ARGUMENTS` in the existing application.

1. Read `CLAUDE.md`, the brand/editorial UI/QA skills, and route-relevant design docs.
2. Inspect the route, loader, dictionaries, metadata, content branches, shared components, CSS, tests, and adjacent routes.
3. Reuse or extend existing primitives before creating a component, helper, hook, grid, dialog, or token.
4. Implement hierarchy, real empty/legacy/fallback/long states, responsive behavior, keyboard/focus, reduced motion, print or media behavior where relevant.
5. Preserve URLs, locale conventions, static generation, feeds/JSON, structured data, CSP, and operator boundaries.
6. Inspect the route at 360, 430, 768, 1024, 1440, and a meaningful wide width; include Czech.
7. Add focused semantic/behavioral tests and run the affected checks.
8. Update design/QA docs only when the shared system changes.
9. Commit a coherent validated route milestone when the task authorizes commits.

Do not return control after an audit when feasible corrections remain.
