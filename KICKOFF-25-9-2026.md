# KICKOFF 2026-09-25 — DNESKAi after the BoardlessAI refocus

Hub: `KICKOFF-25-9-2026.md` in `lukaskourilcz/quorum`. BoardlessAI now works for DNESKAi
and devShark only; MMA Files is paused; the repositories are being renamed. This file is the
order for this repository. Each issue body is self-contained.

## What the research found

| Fact | Where |
| --- | --- |
| The daily missed-day issues (#64 … #94) are an upstream delivery bug: quorum delivers only from its 09:00 Prague retry run, and four September days were never delivered | quorum #555, `.github/workflows/daily.yml:5,85-97` |
| No workflow or runtime code depends on the repository name except the About-page fallback | `lib/config.ts:15`, `README.md:189` |
| Documents say the repository name "stays aifirst"; the package name and env names do stay, the repository name changes | `CLAUDE.md:3,7`, `DOCS.md:4`, `docs/GOVERNANCE.md:4`, `AGENTS.md:7` |
| Both banner slots promote MMA FILES, a paused magazine | `config/banner.json`, `lib/__tests__/banner.test.ts`, `e2e/smoke.spec.ts:120-141` |
| Nothing here produces social content; that stays in quorum (#563 there) | `CLAUDE.md:62,142-143` |

## Order of work

| Step | Issue | When |
| --- | --- | --- |
| A2 | #96 Close the missed-day backlog; one open sentinel issue at most | after quorum #555 is merged and one `cu-day` run has delivered |
| A3 | #97 Banner slots: devShark creatives or empty (owner decision) | after the owner answers |
| A1 | #95 Repository rename to DNESKAi: fallback repo, docs, Vercel env | **after the owner renames the repository** |

## Rules

1. `pnpm verify` and `pnpm e2e` before any push; never claim a check that did not run.
2. The reader shows no production instrumentation and never describes itself as AI-operated.
3. No provider credentials, generation adapters or social assets enter this repository.
4. Keep the `aifirst` package name, the `caught-up` venture id, the skill slugs and the
   compatibility env names; only the GitHub repository name changes.
5. Push or merge only when authorized; this kickoff authorizes pushes to the issue branches.

## Owner actions

- [ ] Answer #97: option A (devShark creatives, approved in quorum's INBOX) or B (empty slots).
- [ ] Rename `lukaskourilcz/aifirst` to `lukaskourilcz/DNESKAi` after quorum #555–#564 and
      own-dashboard #75 are merged; then set `NEXT_PUBLIC_GITHUB_REPO=lukaskourilcz/DNESKAi`
      on the Vercel project and update local remotes.
